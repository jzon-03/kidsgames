import { AdditiondirectiveDirective } from './additiondirective.directive';

describe('AdditiondirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AdditiondirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
